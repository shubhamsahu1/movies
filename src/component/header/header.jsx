import styles from "./header.module.scss";
const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Get to Know about any Movies</h1>
    </header>
  );
};

export default Header;
